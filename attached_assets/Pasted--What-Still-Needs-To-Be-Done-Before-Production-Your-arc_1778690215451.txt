⚠️ What Still Needs To Be Done Before Production
Your architecture is compatible with App Store + Play Store.
But there are still several critical setup steps.
🚨 1. Product IDs Must Match EXACTLY
This is extremely important.
Your:
RevenueCat products
App Store products
Google Play products
tier mapping
must match exactly.
Example:
Plain text
pro_monthly
pro_yearly
elite_monthly
elite_yearly
If mismatched:
purchases fail
offerings disappear
entitlement activation breaks
🚨 2. Your Package Detection Logic Is Fragile
This:
TypeScript
identifier.toLowerCase().includes(tier)
is dangerous.
Instead use explicit mapping:
TypeScript
const PRODUCT_MAP = {
  pro: {
    monthly: 'pro_monthly',
    yearly: 'pro_yearly',
  },
};
This is production-safe.
🚨 3. Google Play Requires Base Plans + Offers
Modern Google Play subscriptions use:
subscriptions
base plans
offers
inside Google Play Console Help⁠�.
RevenueCat supports this, but you must configure it correctly.
🚨 4. App Store Requires Complete Metadata
In App Store Connect⁠� you still need:
subscription groups
localization
review screenshots
trial disclosures
privacy policy
terms URL
Your UI already supports this well.
🚨 5. Sandbox Testing Is Essential
You must test:
purchase success
cancellation
renewal
grace period
billing retry
expired subscriptions
restore purchases
offline behavior
on:
TestFlight
Play Internal Testing
before release.
🚨 6. Webhook Verification Must Be Real
Currently:
TypeScript
return true;
for webhook verification.
That is not production safe.
You must implement:
HMAC validation
signature checking
replay prevention
otherwise attackers could fake subscriptions.
⚠️ 7. Some Store States Aren’t Fully Handled Yet
Google Play especially has states like:
paused
account hold
grace period
billing retry
deferred upgrade
You started supporting:
grace_period
billing_retry
which is very good.
But backend syncing must fully support them too.
⚠️ Syntax Errors Still Exist
Some code snippets are incomplete/broken:
Example:
TypeScript
meta Record<string, string>;
Should be:
TypeScript
meta: Record<string, string>;
Also:
TypeScript
async upsertSubscription( RevenueCatWebhookEvent['event']['data'])
missing parameter name.
Also several Prisma mock sections are malformed.
So:
architecture = excellent
implementation = still needs cleanup
⚠️ isLoading Missing From State
You use:
TypeScript
set({ isLoading: true });
but isLoading does not exist in SubscriptionState.
Type safety would fail.
⚠️ Animated.View Missing Import
Inside TierCard:
TypeScript
<Animated.View
but no:
TypeScript
import Animated from 'react-native-reanimated';
⚠️ RevenueCat CustomerInfo Mapping Is Unsafe
This:
TypeScript
{ entitlements } as any
is not production-safe typing.
You should properly map the full CustomerInfo structure.
⚠️ Backend Sync URL Is Hardcoded
TypeScript
https://api.liquidimpact.com/webhooks/revenuecat
Should come from environment config.
⚠️ Missing Retry + Queue System
Offline purchase sync should eventually include:
retry queue
exponential backoff
failed webhook replay
background sync
Especially because your app is offline-aware
## 1️⃣ `src/features/subscription/types/subscription.types.ts`
 `// src/features/subscription/types/subscription.types.ts   export type SubscriptionTier = 'free' | 'starter' | 'pro' | 'elite' | 'family';   export type BillingCycle = 'monthly' | 'yearly';   export type PurchaseState =      | 'idle'      | 'loading'      | 'purchasing'      | 'success'      | 'error'      | 'restoring'      | 'expired'      | 'trial'     | 'grace_period'     | 'billing_retry';      export interface TierFeature {     id: string;     label: string;     included: boolean;     highlight?: boolean;     tooltip?: string;   }      export interface TierConfig {     id: SubscriptionTier;     name: string;     icon: string;     features: TierFeature[];     scanLimit: string;     highlighted: boolean;     badge?: string;     requiredEntitlement?: string;     sortOrder: number;   }      export interface DynamicPricing {     monthly: { price: number; priceString: string; currency: string };     yearly: { price: number; priceString: string; currency: string; savings: string };     trialDays?: number;     introEligible: boolean;   }      export interface OfferingPackage {     identifier: string;     packageType: 'MONTHLY' | 'ANNUAL' | 'WEEKLY' | 'LIFETIME';     price: number;     priceString: string;     currencyCode: string;     offeringIdentifier: string;     introPrice?: {       price: number;       priceString: string;       period: string;       cycles: number;     };   }      export interface RevenueCatOffering {     identifier: string;     availablePackages: OfferingPackage[];     meta Record<string, string>;     defaultPackage?: OfferingPackage;   }      export interface EntitlementInfo {     identifier: string;     isActive: boolean;     willRenew: boolean;     periodType: 'NORMAL' | 'INTRO' | 'TRIAL';     latestPurchaseDate: string;     originalPurchaseDate: string;     expirationDate: string | null;     store: 'APP_STORE' | 'PLAY_STORE' | 'STRIPE' | 'PROMO' | 'AMAZON';     productIdentifier: string;     ownershipType: 'PURCHASED' | 'FAMILY_SHARED';   }      export interface CustomerEntitlements {     active: Record<string, EntitlementInfo>;     all: Record<string, EntitlementInfo[]>;   }      export interface SubscriptionState {     // Core state     currentTier: SubscriptionTier;     billingCycle: BillingCycle;     entitlements: CustomerEntitlements;     purchaseState: PurchaseState;     errorMessage: string | null;          // Dynamic content     offerings: RevenueCatOffering[];     paywallCopy: Record<string, string>;     experimentVariant: 'A' | 'B' | 'C';          // Cache & persistence     lastOfferingsFetch: number | null;     lastEntitlementSync: number | null;     isOffline: boolean;          // Analytics     viewedAt: number | null;     ctaTappedAt: number | null;     purchaseAttemptedAt: number | null;   }      export interface PurchaseEvent {     userId: string;     tier: SubscriptionTier;     billing: BillingCycle;     price: number;     currency: string;     timestamp: string;     status: 'success' | 'failed' | 'cancelled';     errorCode?: string;     revenueCatTransactionId?: string;   }      export interface PaywallExperiment {     variant: 'A' | 'B' | 'C';     headline?: string;     ctaText?: string;     defaultTier?: SubscriptionTier;     emphasizeYearly?: boolean;     showSocialProof?: boolean;   }   `  
## 2️⃣ `src/features/subscription/animations/motion.ts` (Reanimated 3)
 `// src/features/subscription/animations/motion.ts   import { withSpring, withTiming, interpolate, interpolateColor, runOnJS, useSharedValue, useAnimatedStyle } from 'react-native-reanimated';   import { Platform } from 'react-native';      // Spring physics for natural motion   export const springs = {     gentle: { damping: 15, stiffness: 100, mass: 1, overshootClamping: true },     bouncy: { damping: 10, stiffness: 200, mass: 0.8, overshootClamping: false },     snappy: { damping: 20, stiffness: 300, mass: 0.5, overshootClamping: true },     elastic: { damping: 8, stiffness: 150, mass: 1, overshootClamping: false },   };      // GPU-safe transitions (UI-thread only)   export const transitions = {     fadeIn: { toValue: 1, duration: 300, useNativeDriver: true as const },     fadeOut: { toValue: 0, duration: 200, useNativeDriver: true as const },     scaleIn: { toValue: 1.05, duration: 150, useNativeDriver: true as const },     scaleOut: { toValue: 1, duration: 150, useNativeDriver: true as const },     slideUp: { toValue: 0, duration: 400, useNativeDriver: true as const },     slideDown: { toValue: 100, duration: 400, useNativeDriver: true as const },   };      // Color interpolation for premium gradients   export const interpolateGradient = (     value: ReturnType<typeof useSharedValue<number>>,     colors: [string, string],     progress: number   ) => {     'worklet';     return interpolateColor(       value.value,       [0, 1],       [colors[0], colors[1]]     );   };      // Layout animations (Reanimated 3 Layout)   export const Layout = {     fadeIn: {       entering: { duration: 300, fade: { from: 0, to: 1 } },       exiting: { duration: 200, fade: { from: 1, to: 0 } },     },     slideUp: {       entering: { duration: 400, slide: { from: 'bottom', to: 'top' } },       exiting: { duration: 300, slide: { from: 'top', to: 'bottom' } },     },     scale: {       entering: { duration: 200, scale: { from: 0.95, to: 1 } },       exiting: { duration: 150, scale: { from: 1, to: 0.95 } },     },   };      // Shared value helpers   export const useMotionValue = (initial: number) => {     const value = useSharedValue(initial);     const animateTo = (to: number, config = springs.gentle) => {       value.value = withSpring(to, config);     };     const animateTiming = (to: number, duration = 300) => {       value.value = withTiming(to, { duration });     };     return { value, animateTo, animateTiming };   };      // Animated style factory   export const createAnimatedStyle = (base: any, animations: Record<string, any>) => {     return useAnimatedStyle(() => {       const style: any = { ...base };       Object.entries(animations).forEach(([key, anim]) => {         if (typeof anim === 'function') {           style[key] = anim();         } else {           style[key] = anim;         }       });       return style;     });   };      // Platform-optimized springs   export const getPlatformSpring = () => {     return Platform.OS === 'ios' ? springs.bouncy : springs.gentle;   };   `  
## 3️⃣ `src/features/subscription/services/revenuecat.service.ts` (Real Integration)
 `// src/features/subscription/services/revenuecat.service.ts   import Purchases, {      PurchasesPackage,      CustomerInfo,      PurchasesOffering,      PurchasesOfferings,     LOG_LEVEL    } from 'react-native-purchases';   import { Platform } from 'react-native';   import { RevenueCatOffering, EntitlementInfo, CustomerEntitlements } from '../types/subscription.types';      // Configuration   const REVENUECAT_CONFIG = {     apiKey: process.env.EXPO_PUBLIC_REVENUECAT_API_KEY || '',     appUserId: null as string | null,     observerMode: false,     usesStoreKit2IfAvailable: true,   };      // Service abstraction with real SDK calls   export const RevenueCatService = {     // Initialize SDK with proper configuration     async configure(userId?: string): Promise<void> {       try {         // Set log level for debugging (disable in production)         if (__DEV__) {           Purchases.setLogLevel(LOG_LEVEL.DEBUG);         }                  // Configure with optional app user ID for anonymous → authenticated merging         await Purchases.configure({           apiKey: REVENUECAT_CONFIG.apiKey,           appUserID: userId || REVENUECAT_CONFIG.appUserId || undefined,           observerMode: REVENUECAT_CONFIG.observerMode,           usesStoreKit2IfAvailable: REVENUECAT_CONFIG.usesStoreKit2IfAvailable,         });                  // Set attributes for segmentation         await Purchases.setAttributes({           platform: Platform.OS,           appVersion: '1.0.0', // Replace with actual version         });                  console.log('[RevenueCat] Configured successfully');       } catch (error) {         console.error('[RevenueCat] Configuration failed:', error);         throw new Error('Failed to initialize subscription service');       }     },          // Fetch dynamic offerings with regional pricing     async getOfferings(): Promise<RevenueCatOffering[]> {       try {         const offerings: PurchasesOfferings = await Purchases.getOfferings();         const current = offerings.current;                  if (!current) {           console.warn('[RevenueCat] No current offering available');           return [];         }                  // Map RevenueCat packages to our UI format         const mappedPackages = current.availablePackages.map(pkg => ({           identifier: pkg.identifier,           packageType: pkg.packageType as OfferingPackage['packageType'],           price: pkg.product.price,           priceString: pkg.product.priceString,           currencyCode: pkg.product.currencyCode,           offeringIdentifier: pkg.offeringIdentifier,           introPrice: pkg.product.introPrice ? {             price: pkg.product.introPrice.price,             priceString: pkg.product.introPrice.priceString,             period: pkg.product.introPrice.period,             cycles: pkg.product.introPrice.cycles,           } : undefined,         }));                  return [{           identifier: current.identifier,           availablePackages: mappedPackages,           meta current.metadata,           defaultPackage: mappedPackages.find(p => p.packageType === 'ANNUAL') || mappedPackages[0],         }];       } catch (error) {         console.error('[RevenueCat] Failed to fetch offerings:', error);         throw new Error('Could not load subscription options');       }     },          // Purchase a package with proper error handling     async purchasePackage(packageIdentifier: string): Promise<{ success: boolean; customerInfo: CustomerInfo }> {       try {         // Get current offerings to find the package         const offerings = await Purchases.getOfferings();         const pkg = offerings.current?.availablePackages.find(p => p.identifier === packageIdentifier);                  if (!pkg) {           throw new Error(`Package ${packageIdentifier} not found`);         }                  // Execute purchase         const result = await Purchases.purchasePackage(pkg);                  return {           success: true,           customerInfo: result.customerInfo,         };       } catch (error: any) {         // Handle specific RevenueCat error codes         if (error.code === 'PurchasesErrorCode.PurchaseCancelledError') {           throw new Error('Purchase cancelled by user');         }         if (error.code === 'PurchasesErrorCode.ProductNotAvailableForPurchaseError') {           throw new Error('This subscription is not available in your region');         }         if (error.code === 'PurchasesErrorCode.InvalidAppleSubscriptionKeyError') {           throw new Error('Subscription configuration error. Please contact support.');         }                  console.error('[RevenueCat] Purchase failed:', error);         throw new Error(error.message || 'Purchase failed. Please try again.');       }     },          // Restore purchases (App Store compliance)     async restorePurchases(): Promise<CustomerInfo> {       try {         const customerInfo = await Purchases.restorePurchases();         console.log('[RevenueCat] Purchases restored successfully');         return customerInfo;       } catch (error: any) {         console.error('[RevenueCat] Restore failed:', error);         throw new Error(error.message || 'Could not restore purchases');       }     },          // Get current customer info with entitlements     async getCustomerInfo(): Promise<CustomerInfo | null> {       try {         const customerInfo = await Purchases.getCustomerInfo();         return customerInfo;       } catch (error) {         console.warn('[RevenueCat] Failed to get customer info:', error);         return null;       }     },          // Check intro eligibility for trial logic     async checkIntroEligibility(productIdentifiers: string[]): Promise<Record<string, boolean>> {       try {         return await Purchases.checkTrialOrIntroductoryPriceEligibility(productIdentifiers);       } catch (error) {         console.warn('[RevenueCat] Eligibility check failed:', error);         return {};       }     },          // Sync customer info to backend via webhook pattern     async syncToBackend(customerInfo: CustomerInfo, userId: string): Promise<void> {       try {         // In production: POST to your backend webhook endpoint         const response = await fetch('https://api.liquidimpact.com/webhooks/revenuecat', {           method: 'POST',           headers: { 'Content-Type': 'application/json' },           body: JSON.stringify({             userId,             event: 'customer_info_updated',             customerInfo: {               originalAppUserId: customerInfo.originalAppUserId,               entitlements: customerInfo.entitlements,               firstSeen: customerInfo.firstSeen,               originalApplicationVersion: customerInfo.originalApplicationVersion,             },             timestamp: new Date().toISOString(),           }),         });                  if (!response.ok) {           console.warn('[Backend] Sync failed:', await response.text());         }       } catch (error) {         console.error('[Backend] Sync error:', error);         // Don't throw - this is best-effort sync       }     },          // Listen for entitlement changes (real-time updates)     onEntitlementChanged(callback: (entitlements: CustomerEntitlements) => void): () => void {       const listener = Purchases.addCustomerInfoUpdateListener((customerInfo) => {         const mapped: CustomerEntitlements = {           active: customerInfo.entitlements.active,           all: customerInfo.entitlements.all,         };         callback(mapped);       });              // Return cleanup function       return () => Purchases.removeCustomerInfoUpdateListener(listener);     },          // Log out anonymous user (for auth flow)     async logOut(): Promise<CustomerInfo> {       return await Purchases.logOut();     },          // Log in authenticated user (merge anonymous data)     async logIn(userId: string): Promise<{ customerInfo: CustomerInfo; created: boolean }> {       return await Purchases.logIn(userId);     },   };      // Type-safe wrapper for common operations   export const useRevenueCat = () => {     const configure = async (userId?: string) => {       await RevenueCatService.configure(userId);     };          const purchase = async (packageId: string) => {       return await RevenueCatService.purchasePackage(packageId);     };          const restore = async () => {       return await RevenueCatService.restorePurchases();     };          const getEntitlements = async () => {       const info = await RevenueCatService.getCustomerInfo();       return info?.entitlements || null;     };          return { configure, purchase, restore, getEntitlements };   };   `  
## 4️⃣ `src/features/subscription/store/subscription.store.ts` (Zustand + Persistence)
 `// src/features/subscription/store/subscription.store.ts   import { create } from 'zustand';   import { persist, createJSONStorage } from 'zustand/middleware';   import AsyncStorage from '@react-native-async-storage/async-storage';   import { SubscriptionState, SubscriptionTier, BillingCycle, PurchaseState, CustomerEntitlements, RevenueCatOffering, PaywallExperiment } from '../types/subscription.types';   import { RevenueCatService } from '../services/revenuecat.service';   import { SubscriptionCacheService } from '../services/subscription-cache.service';      // Initial state   const initialState: SubscriptionState = {     currentTier: 'free',     billingCycle: 'yearly',     entitlements: { active: {}, all: {} },     purchaseState: 'idle',     errorMessage: null,     offerings: [],     paywallCopy: {},     experimentVariant: 'A',     lastOfferingsFetch: null,     lastEntitlementSync: null,     isOffline: false,     viewedAt: null,     ctaTappedAt: null,     purchaseAttemptedAt: null,   };      // Store definition   interface SubscriptionStore extends SubscriptionState {     // Actions     initialize: (userId?: string) => Promise<void>;     refreshOfferings: () => Promise<void>;     refreshEntitlements: () => Promise<void>;     selectTier: (tier: SubscriptionTier) => void;     selectBilling: (cycle: BillingCycle) => void;     purchase: (tier: SubscriptionTier, billing: BillingCycle) => Promise<void>;     restorePurchases: () => Promise<void>;     setError: (message: string | null) => void;     setPurchaseState: (state: PurchaseState) => void;     updateExperiment: (variant: PaywallExperiment) => void;     trackView: () => void;     trackCTA: () => void;     trackPurchaseAttempt: () => void;     syncToBackend: () => Promise<void>;     reset: () => void;          // Selectors (computed)     isEntitledTo: (feature: string) => boolean;     getPackageFor: (tier: SubscriptionTier, billing: BillingCycle) => RevenueCatOffering['availablePackages'][0] | null;     getDynamicPrice: (tier: SubscriptionTier) => { price: string; period: string; savings?: string };     shouldShowTrial: () => boolean;     isExpired: () => boolean;     isInGracePeriod: () => boolean;   }      // Create store with persistence   export const useSubscriptionStore = create<SubscriptionStore>()(     persist(       (set, get) => ({         ...initialState,                  // Initialize: configure RevenueCat + load cached data         initialize: async (userId?: string) => {           try {             set({ isLoading: true });                          // 1. Configure RevenueCat             await RevenueCatService.configure(userId);                          // 2. Load cached offerings (if fresh)             const cached = await SubscriptionCacheService.getOfferings();             if (cached && SubscriptionCacheService.isFresh(cached.timestamp)) {               set({ offerings: cached.offerings, lastOfferingsFetch: cached.timestamp });             }                          // 3. Load cached entitlements             const cachedEntitlements = await SubscriptionCacheService.getEntitlements();             if (cachedEntitlements) {               set({ entitlements: cachedEntitlements });             }                          // 4. Refresh in background             get().refreshOfferings();             get().refreshEntitlements();                        } catch (error) {             console.error('[Store] Initialization failed:', error);             set({ errorMessage: 'Could not load subscription data' });           } finally {             set({ isLoading: false });           }         },                  // Refresh offerings from RevenueCat         refreshOfferings: async () => {           try {             const offerings = await RevenueCatService.getOfferings();             set({ offerings, lastOfferingsFetch: Date.now() });                          // Cache for offline use             await SubscriptionCacheService.cacheOfferings(offerings);           } catch (error) {             console.warn('[Store] Failed to refresh offerings:', error);             // Use cached data if available             const cached = await SubscriptionCacheService.getOfferings();             if (cached) {               set({ offerings: cached.offerings });             }           }         },                  // Refresh entitlements from RevenueCat + sync to backend         refreshEntitlements: async () => {           try {             const customerInfo = await RevenueCatService.getCustomerInfo();             if (customerInfo) {               const entitlements: CustomerEntitlements = {                 active: customerInfo.entitlements.active,                 all: customerInfo.entitlements.all,               };                              set({ entitlements, lastEntitlementSync: Date.now() });                              // Sync to backend               await get().syncToBackend();                              // Cache for offline               await SubscriptionCacheService.cacheEntitlements(entitlements);             }           } catch (error) {             console.warn('[Store] Failed to refresh entitlements:', error);           }         },                  // Select tier (UI only)         selectTier: (tier: SubscriptionTier) => {           set({ currentTier: tier });         },                  // Select billing cycle (UI only)         selectBilling: (cycle: BillingCycle) => {           set({ billingCycle: cycle });         },                  // Purchase flow with state management         purchase: async (tier: SubscriptionTier, billing: BillingCycle) => {           if (tier === 'free') return;                      set({ purchaseState: 'purchasing', errorMessage: null, purchaseAttemptedAt: Date.now() });                      try {             // Get package from offerings             const pkg = get().getPackageFor(tier, billing);             if (!pkg) throw new Error('Subscription package not available');                          // Execute purchase             const result = await RevenueCatService.purchasePackage(pkg.identifier);                          // Update state on success             set({               currentTier: tier,               entitlements: {                 active: result.customerInfo.entitlements.active,                 all: result.customerInfo.entitlements.all,               },               purchaseState: 'success',             });                          // Sync to backend             await get().syncToBackend();                        } catch (error: any) {             set({               purchaseState: 'error',               errorMessage: error.message || 'Purchase failed',             });             throw error;           }         },                  // Restore purchases         restorePurchases: async () => {           set({ purchaseState: 'restoring', errorMessage: null });                      try {             const customerInfo = await RevenueCatService.restorePurchases();                          set({               entitlements: {                 active: customerInfo.entitlements.active,                 all: customerInfo.entitlements.all,               },               purchaseState: customerInfo.entitlements.active.pro ? 'success' : 'expired',             });                          await get().syncToBackend();                        } catch (error: any) {             set({               purchaseState: 'error',               errorMessage: error.message || 'Restore failed',             });           }         },                  // Error handling         setError: (message: string | null) => {           set({ errorMessage: message });         },                  setPurchaseState: (state: PurchaseState) => {           set({ purchaseState: state });         },                  // Experiment/variant support         updateExperiment: (variant: PaywallExperiment) => {           set({             experimentVariant: variant.variant,             paywallCopy: {               ...get().paywallCopy,               ...(variant.headline && { headline: variant.headline }),               ...(variant.ctaText && { ctaText: variant.ctaText }),             },           });         },                  // Analytics tracking         trackView: () => set({ viewedAt: Date.now() }),         trackCTA: () => set({ ctaTappedAt: Date.now() }),         trackPurchaseAttempt: () => set({ purchaseAttemptedAt: Date.now() }),                  // Backend sync         syncToBackend: async () => {           const { entitlements } = get();           const userId = 'current_user_id'; // Replace with auth context                      await RevenueCatService.syncToBackend(             { entitlements } as any, // Map to CustomerInfo shape             userId           );         },                  // Reset state         reset: () => set(initialState),                  // ── SELECTORS (computed values) ──                  isEntitledTo: (feature: string) => {           const { entitlements } = get();           // Map feature to entitlement identifier           const entitlementMap: Record<string, string> = {             'unlimited_scans': 'pro',             'advanced_ai': 'elite',             'family_sharing': 'family',           };           const entitlementId = entitlementMap[feature];           return !!(entitlementId && entitlements.active[entitlementId]?.isActive);         },                  getPackageFor: (tier: SubscriptionTier, billing: BillingCycle) => {           const { offerings } = get();           const offering = offerings[0];           if (!offering) return null;                      const packageType = billing === 'monthly' ? 'MONTHLY' : 'ANNUAL';           return offering.availablePackages.find(p =>              p.packageType === packageType &&              p.identifier.toLowerCase().includes(tier)           ) || null;         },                  getDynamicPrice: (tier: SubscriptionTier) => {           const { offerings, billingCycle } = get();           const offering = offerings[0];                      if (tier === 'free') return { price: '$0', period: 'forever' };                      // Try RevenueCat dynamic pricing           if (offering) {             const pkg = offering.availablePackages.find(p =>                (billingCycle === 'monthly' && p.packageType === 'MONTHLY') ||               (billingCycle === 'yearly' && p.packageType === 'ANNUAL')             );             if (pkg) {               return {                 price: pkg.priceString,                 period: billingCycle === 'monthly' ? '/month' : '/year',                 savings: billingCycle === 'yearly' ? pkg.introPrice ? `Save with intro offer` : 'Save up to 50%' : undefined,               };             }           }                      // Fallback to hardcoded (should rarely happen)           const fallback: Record<SubscriptionTier, { monthly: string; yearly: string }> = {             free: { monthly: '$0', yearly: '$0' },             starter: { monthly: '$3.99', yearly: '$24.99' },             pro: { monthly: '$7.99', yearly: '$49.99' },             elite: { monthly: '$14.99', yearly: '$99.99' },             family: { monthly: '$19.99', yearly: '$119.99' },           };           return {             price: fallback[tier][billingCycle],             period: billingCycle === 'monthly' ? '/month' : '/year',             savings: billingCycle === 'yearly' ? 'Save up to 50%' : undefined,           };         },                  shouldShowTrial: () => {           const { entitlements, currentTier } = get();           // Show trial if user has no active paid entitlement           const hasActivePaid = Object.values(entitlements.active).some(e =>              e.isActive && e.identifier !== 'free'           );           return !hasActivePaid && currentTier !== 'free';         },                  isExpired: () => {           const { entitlements, currentTier } = get();           if (currentTier === 'free') return false;                      const entitlement = entitlements.active[currentTier];           return !!(entitlement && !entitlement.isActive && entitlement.expirationDate);         },                  isInGracePeriod: () => {           const { purchaseState } = get();           return purchaseState === 'grace_period' || purchaseState === 'billing_retry';         },       }),       {         name: 'liquid-subscription-store',         storage: createJSONStorage(() => AsyncStorage),         partialize: (state) => ({           // Persist only essential state           currentTier: state.currentTier,           billingCycle: state.billingCycle,           entitlements: state.entitlements,           lastOfferingsFetch: state.lastOfferingsFetch,           lastEntitlementSync: state.lastEntitlementSync,           experimentVariant: state.experimentVariant,         }),         version: 1,         migrate: (persistedState: any, version: number) => {           // Handle store version migrations           if (version === 0) {             // Add new fields for v1             persistedState.isOffline = false;             persistedState.viewedAt = null;           }           return persistedState;         },       }     )   );      // Performance-optimized selectors   export const selectCurrentTier = (s: SubscriptionStore) => s.currentTier;   export const selectBillingCycle = (s: SubscriptionStore) => s.billingCycle;   export const selectEntitlements = (s: SubscriptionStore) => s.entitlements;   export const selectPurchaseState = (s: SubscriptionStore) => s.purchaseState;   export const selectErrorMessage = (s: SubscriptionStore) => s.errorMessage;   export const selectOfferings = (s: SubscriptionStore) => s.offerings;   export const selectIsEntitled = (feature: string) => (s: SubscriptionStore) => s.isEntitledTo(feature);   export const selectDynamicPrice = (tier: SubscriptionTier) => (s: SubscriptionStore) => s.getDynamicPrice(tier);   export const selectShouldShowTrial = (s: SubscriptionStore) => s.shouldShowTrial();   `  
## 5️⃣ `src/features/subscription/components/TierCard.tsx` (Reanimated + Memoized)
 `// src/features/subscription/components/TierCard.tsx   import React, { memo, useCallback } from 'react';   import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';   import { Ionicons } from '@expo/vector-icons';   import {      useAnimatedStyle,      useSharedValue,      withSpring,      withTiming,      interpolate,     interpolateColor,   } from 'react-native-reanimated';   import * as Haptics from 'expo-haptics';   import { TierConfig } from '../types/subscription.types';   import { springs, createAnimatedStyle } from '../animations/motion';   import { useColors } from '@/shared/theme/useColors';   import { FeatureReveal } from './FeatureReveal';      interface TierCardProps {     tier: TierConfig;     isSelected: boolean;     billing: 'monthly' | 'yearly';     price: string;     period: string;     savings?: string;     onSelect: (tier: string) => void;     onFeaturePress?: (featureId: string) => void;   }      export const TierCard: React.FC<TierCardProps> = memo(({     tier,     isSelected,     billing,     price,     period,     savings,     onSelect,     onFeaturePress,   }) => {     const colors = useColors();          // Animation values     const scale = useSharedValue(1);     const borderColor = useSharedValue(0); // 0 = default, 1 = active          // Tier-specific color     const tierColors: Record<string, { primary: string; secondary: string }> = {       free: { primary: '#6B6B80', secondary: '#3A3A4A' },       starter: { primary: '#00B4D8', secondary: '#007791' },       pro: { primary: '#7B2CBF', secondary: '#5A189A' },       elite: { primary: '#FFD700', secondary: '#B8860B' },       family: { primary: '#FF6B9D', secondary: '#C9184A' },     };     const { primary, secondary } = tierColors[tier.id] || tierColors.free;          // GPU-safe press animation     const handlePressIn = useCallback(() => {       scale.value = withSpring(0.98, springs.snappy);       Haptics.selectionAsync();     }, [scale]);          const handlePressOut = useCallback(() => {       scale.value = withSpring(1, springs.snappy);     }, [scale]);          // Animated border color interpolation     const animatedBorder = useAnimatedStyle(() => {       return {         borderColor: interpolateColor(           borderColor.value,           [0, 1],           [colors.border, primary]         ),       };     });          // Update border color when selection changes     React.useEffect(() => {       borderColor.value = withTiming(isSelected ? 1 : 0, { duration: 200 });     }, [isSelected, borderColor, primary]);          // Animated container style     const animatedContainer = createAnimatedStyle(       { transform: [{ scale }] },       {}     );          const handleSelect = useCallback(() => {       onSelect(tier.id);     }, [onSelect, tier.id]);          return (       <Animated.View style={[styles.container, animatedContainer]}>         <TouchableOpacity           onPress={handleSelect}           onPressIn={handlePressIn}           onPressOut={handlePressOut}           activeOpacity={0.9}           accessibilityRole="radio"           accessibilityState={{ selected: isSelected }}           accessibilityLabel={`${tier.name} tier - ${price}${period}`}           style={[             styles.card,             {                backgroundColor: tier.highlighted                  ? `${primary}10`                  : `${colors.backgroundSecondary}CC`,             },             animatedBorder,           ]}         >           {/* Header: Icon + Title + Price + Radio */}           <View style={styles.header}>             <View style={[styles.icon, { backgroundColor: `${primary}18`, borderColor: `${primary}30` }]}>               <Ionicons name={tier.icon as any} size={22} color={primary} />             </View>                          <View style={styles.titleGroup}>               <View style={styles.titleRow}>                 <Text style={[styles.name, { color: colors.foreground }]} numberOfLines={1}>                   {tier.name}                 </Text>                 {tier.badge && (                   <View style={[styles.badge, { backgroundColor: `${colors.success}15`, borderColor: `${colors.success}30` }]}>                     <Text style={[styles.badgeText, { color: colors.success }]}>{tier.badge}</Text>                   </View>                 )}               </View>                              <View style={styles.priceRow}>                 <Text style={[styles.price, { color: primary }]}>{price}</Text>                 <Text style={[styles.period, { color: colors.mutedForeground }]}>                   {tier.id === 'free' ? 'forever' : period}                 </Text>               </View>                              {billing === 'yearly' && savings && (                 <Text style={[styles.savings, { color: colors.success }]}>{savings}</Text>               )}                              <Text style={[styles.scanLimit, { color: colors.mutedForeground }]}>                 {tier.scanLimit} scans               </Text>             </View>                          {/* Custom radio button */}             <View style={[styles.radio, { borderColor: isSelected ? primary : 'rgba(255,255,255,0.2)' }]}>               {isSelected && (                 <View style={[styles.radioDot, { backgroundColor: primary }]} />               )}             </View>           </View>                      {/* Features (staggered reveal when selected) */}           {isSelected && (             <FeatureReveal features={tier.features} onFeaturePress={onFeaturePress} />           )}         </TouchableOpacity>       </Animated.View>     );   }, (prev, next) =>      prev.isSelected === next.isSelected &&      prev.billing === next.billing &&     prev.tier.id === next.tier.id &&     prev.price === next.price   );      TierCard.displayName = 'TierCard';      const styles = StyleSheet.create({     container: { marginBottom: 12 },     card: { borderRadius: 24, padding: 18, borderWidth: 1 },     header: { flexDirection: 'row', alignItems: 'flex-start', gap: 12 },     icon: {        width: 48, height: 48, borderRadius: 14, borderWidth: 1,        justifyContent: 'center', alignItems: 'center'      },     titleGroup: { flex: 1 },     titleRow: { flexDirection: 'row', alignItems: 'center', gap: 8, marginBottom: 2 },     name: { fontSize: 17, fontWeight: '800' },     badge: { paddingHorizontal: 8, paddingVertical: 2, borderRadius: 6, borderWidth: 1 },     badgeText: { fontSize: 10, fontWeight: '700' },     priceRow: { flexDirection: 'row', alignItems: 'baseline', gap: 3 },     price: { fontSize: 22, fontWeight: '800' },     period: { fontSize: 12 },     savings: { fontSize: 11, fontWeight: '600', marginTop: 2 },     scanLimit: { fontSize: 11, marginTop: 2 },     radio: {        width: 24, height: 24, borderRadius: 12, borderWidth: 2,        justifyContent: 'center', alignItems: 'center'      },     radioDot: { width: 12, height: 12, borderRadius: 6 },   });   `  
## 6️⃣ `src/features/subscription/screens/PaywallScreen.tsx` (Clean Orchestrator ~120 lines)
 `// src/features/subscription/screens/PaywallScreen.tsx   // ✅ Clean orchestrator: UI composition only, all logic in hooks/services      import React, { useEffect, useCallback } from 'react';   import { View, ScrollView, StyleSheet, TouchableOpacity, Linking, Platform } from 'react-native';   import { useSafeAreaInsets } from 'react-native-safe-area-context';   import { useRouter } from 'expo-router';   import { Ionicons } from '@expo/vector-icons';   import { Layout } from 'react-native-reanimated';      // ── Architecture imports ──   import { useColors } from '@/shared/theme/useColors';   import { useApp } from '@/context/AppContext';   import { useSubscriptionStore, selectPurchaseState, selectErrorMessage, selectCurrentTier, selectBillingCycle, selectDynamicPrice } from '../store/subscription.store';   import { useRevenueCat } from '../hooks/useRevenueCat';   import { usePurchaseFlow } from '../hooks/usePurchaseFlow';   import { usePaywallExperiment } from '../hooks/usePaywallExperiment';   import { useSubscriptionAnalytics } from '../hooks/useSubscriptionAnalytics';      // ── Component imports ──   import { AmbientBackground } from '../components/AmbientBackground';   import { ScanHeader } from '../components/ScanHeader';   import { BillingToggle } from '../components/BillingToggle';   import { TierCard } from '../components/TierCard';   import { AnimatedCTA } from '../components/AnimatedCTA';   import { TrustIndicators } from '../components/TrustIndicators';   import { PremiumErrorModal } from '../components/PremiumErrorModal';   import { SuccessAnimation } from '../components/SuccessAnimation';   import { TIERS } from '../constants/tiers';   import { COMPLIANCE } from '../constants/compliance';      export default function PaywallScreen() {     // ── Dependencies ──     const colors = useColors();     const insets = useSafeAreaInsets();     const router = useRouter();     const { state: appState } = useApp();          // ── Store selectors (performance-optimized) ──     const purchaseState = useSubscriptionStore(selectPurchaseState);     const errorMessage = useSubscriptionStore(selectErrorMessage);     const currentTier = useSubscriptionStore(selectCurrentTier);     const billingCycle = useSubscriptionStore(selectBillingCycle);     const getDynamicPrice = useSubscriptionStore(selectDynamicPrice);          // ── Hooks ──     const { configure: configureRevenueCat } = useRevenueCat();     const { selectedTier, billing, handleSelectTier, handleToggleBilling, handleSubscribe, handleRestore } = usePurchaseFlow();     const { variant, headline, ctaText } = usePaywallExperiment();     const { trackView, trackCTA, trackPurchaseAttempt, trackSuccess, trackError } = useSubscriptionAnalytics();          // ── Local state ──     const [showErrorModal, setShowErrorModal] = React.useState(false);     const [showSuccess, setShowSuccess] = React.useState(false);          // ── Lifecycle ──     useEffect(() => {       // Initialize RevenueCat + store       configureRevenueCat();       trackView();     }, [configureRevenueCat, trackView]);          // ── Effects ──     useEffect(() => {       if (purchaseState === 'success') {         setShowSuccess(true);         trackSuccess();       } else if (purchaseState === 'error' && errorMessage) {         setShowErrorModal(true);         trackError(errorMessage);       }     }, [purchaseState, errorMessage, trackSuccess, trackError]);          // ── Handlers ──     const handleSuccessComplete = useCallback(() => {       setShowSuccess(false);       router.replace('/');     }, [router]);          const handleCTAPress = useCallback(() => {       trackCTA();       trackPurchaseAttempt();       handleSubscribe();     }, [trackCTA, trackPurchaseAttempt, handleSubscribe]);          const handleManageSubscription = useCallback(async () => {       const url = Platform.OS === 'ios'          ? 'https://apps.apple.com/account/subscriptions'         : 'https://play.google.com/store/account/subscriptions';       await Linking.openURL(url);     }, []);          // ── Derived values ──     const { price, period, savings } = getDynamicPrice(selectedTier);     const isProcessing = purchaseState === 'purchasing' || purchaseState === 'loading' || purchaseState === 'restoring';          // ── Render: Success State ──     if (showSuccess) {       return (         <View style={{ flex: 1, backgroundColor: colors.background }}>           <AmbientBackground variant="success" />           <SuccessAnimation onComplete={handleSuccessComplete} />         </View>       );     }          // ── Render: Main Flow ──     return (       <View style={{ flex: 1, backgroundColor: colors.background }}>         <AmbientBackground variant="paywall" />                  {/* Close button */}         <TouchableOpacity           onPress={() => router.back()}           style={{ position: 'absolute', top: insets.top + 12, right: 20, zIndex: 10, width: 36, height: 36, borderRadius: 18, backgroundColor: 'rgba(255,255,255,0.1)', justifyContent: 'center', alignItems: 'center' }}           accessibilityLabel="Close"           accessibilityRole="button"         >           <Ionicons name="close" size={18} color="#fff" />         </TouchableOpacity>                  <ScrollView           showsVerticalScrollIndicator={false}           contentContainerStyle={{             paddingTop: insets.top + 56,             paddingBottom: insets.bottom + 32,             paddingHorizontal: 20,             gap: 20,           }}         >           {/* Header with experiment variant */}           <ScanHeader              headline={headline}              subheadline="See exactly what your drinks do to your body"           />                      {/* Billing toggle with elastic animation */}           <BillingToggle              billing={billing}              onToggle={handleToggleBilling}              emphasizeYearly={variant.emphasizeYearly}           />                      {/* Tier cards (memoized) */}           {TIERS.map((tier) => (             <TierCard               key={tier.id}               tier={tier}               isSelected={selectedTier === tier.id}               billing={billing}               price={tier.id === 'free' ? '$0' : getDynamicPrice(tier.id).price}               period={tier.id === 'free' ? 'forever' : billing === 'monthly' ? '/month' : '/year'}               savings={billing === 'yearly' && tier.id !== 'free' ? getDynamicPrice(tier.id).savings : undefined}               onSelect={handleSelectTier}             />           ))}                      {/* Animated CTA button with shimmer */}           <AnimatedCTA             text={ctaText || `Get ${selectedTier} — ${price}${period}`}             isProcessing={isProcessing}             isFree={selectedTier === 'free'}             onPress={handleCTAPress}             variant={variant.variant}           />                      {/* Trust indicators + compliance */}           <TrustIndicators />                      {/* Legal footer with dynamic trial text */}           <View style={{ gap: 8 }} accessibilityLabel="Legal information">             <Text style={{ color: colors.mutedForeground, fontSize: 11, textAlign: 'center' }}>               {COMPLIANCE.footer}             </Text>                          {selectedTier !== 'free' && (               <Text style={{ color: colors.subtext, fontSize: 10, textAlign: 'center' }}>                 {COMPLIANCE.trialDisclosure.replace('{price}', price).replace('{period}', period)}               </Text>             )}                          <View style={{ flexDirection: 'row', justifyContent: 'center', gap: 16 }}>               <TouchableOpacity onPress={() => {}} accessibilityLabel="Terms" accessibilityRole="link">                 <Text style={{ color: colors.primary, fontSize: 11 }}>{COMPLIANCE.terms}</Text>               </TouchableOpacity>               <TouchableOpacity onPress={() => {}} accessibilityLabel="Privacy" accessibilityRole="link">                 <Text style={{ color: colors.primary, fontSize: 11 }}>{COMPLIANCE.privacy}</Text>               </TouchableOpacity>               <TouchableOpacity onPress={handleManageSubscription} accessibilityLabel="Manage" accessibilityRole="link">                 <Text style={{ color: colors.primary, fontSize: 11 }}>{COMPLIANCE.manage}</Text>               </TouchableOpacity>             </View>           </View>                      {/* Restore purchases (App Store compliance) */}           <TouchableOpacity              onPress={handleRestore}              style={{ alignItems: 'center', paddingVertical: 8 }}             accessibilityLabel="Restore purchases"             accessibilityRole="button"             disabled={isProcessing}           >             <Text style={{ color: colors.mutedForeground, fontSize: 13 }}>               {purchaseState === 'restoring' ? 'Restoring...' : COMPLIANCE.restore}             </Text>           </TouchableOpacity>         </ScrollView>                  {/* Premium error modal */}         <PremiumErrorModal           visible={showErrorModal}           title="Purchase Failed"           message={errorMessage || 'Something went wrong. Please try again.'}           retryLabel="Retry"           onRetry={() => { setShowErrorModal(false); handleSubscribe(); }}           onCancel={() => { setShowErrorModal(false); }}         />       </View>     );   }      const styles = StyleSheet.create({     // Minimal styles - most handled by components   });   `  
## 7️⃣ `src/features/subscription/hooks/usePurchaseFlow.ts` (State Machine)
 `// src/features/subscription/hooks/usePurchaseFlow.ts   import { useCallback, useState, useEffect } from 'react';   import * as Haptics from 'expo-haptics';   import { useSubscriptionStore } from '../store/subscription.store';   import { useRevenueCat } from './useRevenueCat';   import { useSubscriptionAnalytics } from './useSubscriptionAnalytics';   import { SubscriptionTier, BillingCycle } from '../types/subscription.types';      export const usePurchaseFlow = () => {     // Store access     const {        currentTier,        billingCycle,        selectTier,        selectBilling,        purchase,        restorePurchases,       setError,     } = useSubscriptionStore();          // Service hooks     const { purchase: purchaseWithRevenueCat } = useRevenueCat();     const { trackTierSelect, trackBillingToggle, trackPurchaseStart, trackPurchaseSuccess, trackPurchaseError } = useSubscriptionAnalytics();          // Local state for UI     const [selectedTier, setSelectedTier] = useState<SubscriptionTier>(currentTier);     const [billing, setBilling] = useState<BillingCycle>(billingCycle);          // Sync local state with store     useEffect(() => {       setSelectedTier(currentTier);       setBilling(billingCycle);     }, [currentTier, billingCycle]);          // Tier selection with haptics + analytics     const handleSelectTier = useCallback((tier: SubscriptionTier) => {       setSelectedTier(tier);       selectTier(tier);       trackTierSelect(tier);       Haptics.selectionAsync();     }, [selectTier, trackTierSelect]);          // Billing toggle with haptics + analytics     const handleToggleBilling = useCallback((cycle: BillingCycle) => {       setBilling(cycle);       selectBilling(cycle);       trackBillingToggle(cycle);       Haptics.selectionAsync();     }, [selectBilling, trackBillingToggle]);          // Purchase flow with error handling     const handleSubscribe = useCallback(async () => {       if (selectedTier === 'free') {         // Free tier: just navigate back         return;       }              try {         trackPurchaseStart(selectedTier, billing);         await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);                  // Execute purchase via store (which calls RevenueCat)         await purchase(selectedTier, billing);                  // Success handled by store effect       } catch (error: any) {         trackPurchaseError(selectedTier, billing, error.message);         setError(error.message);         await Haptics.notificationAsync(Haptics.NotificationFeedbackType.Error);       }     }, [selectedTier, billing, purchase, trackPurchaseStart, trackPurchaseError, setError]);          // Restore purchases     const handleRestore = useCallback(async () => {       try {         await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);         await restorePurchases();       } catch (error: any) {         setError(error.message);         await Haptics.notificationAsync(Haptics.NotificationFeedbackType.Error);       }     }, [restorePurchases, setError]);          return {       selectedTier,       billing,       handleSelectTier,       handleToggleBilling,       handleSubscribe,       handleRestore,     };   };   `  
## 8️⃣ `src/features/subscription/services/backend-sync.service.ts` (Prisma Webhook Handler)
 `// src/features/subscription/services/backend-sync.service.ts   // Backend webhook handler for RevenueCat events → Prisma DB sync      import { EntitlementInfo, CustomerEntitlements } from '../types/subscription.types';      // Webhook event types from RevenueCat   export interface RevenueCatWebhookEvent {     event: {       id: string;       created_at: string;       type:          | 'INITIAL_PURCHASE'         | 'RENEWAL'         | 'PRODUCT_CHANGE'         | 'CANCELLATION'         | 'UNCANCELLATION'         | 'BILLING_ISSUE'         | 'EXPIRATION'         | 'REFUND'         | 'SUBSCRIPTION_PAUSED'         | 'SUBSCRIPTION_RESUMED'         | 'TRANSFER';        {         id: string;         user_id: string;         product_id: string;         store: 'APP_STORE' | 'PLAY_STORE' | 'STRIPE' | 'AMAZON';         purchased_at_ms: number;         expires_at_ms: number | null;         period_type: 'NORMAL' | 'INTRO' | 'TRIAL';         is_trial_conversion: boolean;         offer_code: string | null;         present_offered_price: boolean;         price: {           amount: number;           currency: string;         };         subscriber_attributes: Record<string, { value: string; updated_at_ms: number }>;       };     };     api_version: string;   }      // Sync service for backend   export const BackendSyncService = {     // Handle incoming webhook from RevenueCat     async handleWebhook(event: RevenueCatWebhookEvent): Promise<{ success: boolean; message: string }> {       try {         const { type, data } = event.event;                  // Map event to Prisma operations         switch (type) {           case 'INITIAL_PURCHASE':           case 'RENEWAL':           case 'PRODUCT_CHANGE':             await this.upsertSubscription(data);             await this.createAuditLog('PURCHASE', data);             break;                        case 'CANCELLATION':           case 'EXPIRATION':             await this.updateSubscriptionStatus(data.user_id, data.product_id, 'CANCELLED');             await this.createAuditLog('CANCELLED', data);             break;                        case 'REFUND':             await this.updateSubscriptionStatus(data.user_id, data.product_id, 'REFUNDED');             await this.createAuditLog('REFUND', data);             break;                        case 'BILLING_ISSUE':             await this.updateSubscriptionStatus(data.user_id, data.product_id, 'GRACE_PERIOD');             await this.createAuditLog('BILLING_ISSUE', data);             break;                        default:             console.log(`[Webhook] Unhandled event type: ${type}`);         }                  return { success: true, message: 'Webhook processed' };                } catch (error) {         console.error('[Webhook] Processing failed:', error);         return { success: false, message: 'Processing error' };       }     },          // Upsert subscription in Prisma DB     async upsertSubscription( RevenueCatWebhookEvent['event']['data']): Promise<void> {       // In production: Use Prisma client       // await prisma.subscription.upsert({       //   where: {       //     userId_productId: {       //       userId: data.user_id,       //       productId: data.product_id,       //     },       //   },       //   update: {       //     status: 'ACTIVE',       //     expiresAt: data.expires_at_ms ? new Date(data.expires_at_ms) : null,       //     periodType: data.period_type,       //     lastRenewedAt: new Date(data.purchased_at_ms),       //     price: data.price.amount,       //     currency: data.price.currency,       //     store: data.store,       //   },       //   create: {       //     userId: data.user_id,       //     productId: data.product_id,       //     status: 'ACTIVE',       //     startsAt: new Date(data.purchased_at_ms),       //     expiresAt: data.expires_at_ms ? new Date(data.expires_at_ms) : null,       //     periodType: data.period_type,       //     price: data.price.amount,       //     currency: data.price.currency,       //     store: data.store,       //     isTrial: data.period_type === 'TRIAL',       //   },       // });       console.log(`[Prisma] Upserted subscription for user ${data.user_id}`);     },          // Update subscription status     async updateSubscriptionStatus(userId: string, productId: string, status: string): Promise<void> {       // await prisma.subscription.updateMany({       //   where: { userId, productId },       //    { status, updatedAt: new Date() },       // });       console.log(`[Prisma] Updated ${productId} for user ${userId} to ${status}`);     },          // Create audit log for compliance     async createAuditLog(eventType: string, data: RevenueCatWebhookEvent['event']['data']): Promise<void> {       // await prisma.subscriptionAudit.create({       //    {       //     userId: data.user_id,       //     eventType,       //     productId: data.product_id,       //     revenueCatEventId: data.id,       //     price: data.price.amount,       //     currency: data.price.currency,       //     timestamp: new Date(data.purchased_at_ms),       //     meta {       //       store: data.store,       //       periodType: data.period_type,       //       isTrialConversion: data.is_trial_conversion,       //     },       //   },       // });       console.log(`[Prisma] Created audit log for ${eventType}`);     },          // Get user entitlements from DB (fallback if RevenueCat unavailable)     async getUserEntitlements(userId: string): Promise<CustomerEntitlements> {       // const subscriptions = await prisma.subscription.findMany({       //   where: { userId, status: 'ACTIVE' },       // });              // Map to RevenueCat shape       const active: Record<string, EntitlementInfo> = {};       // subscriptions.forEach(sub => {       //   active[sub.productId] = {       //     identifier: sub.productId,       //     isActive: true,       //     willRenew: true,       //     periodType: sub.periodType as any,       //     latestPurchaseDate: sub.lastRenewedAt?.toISOString() || '',       //     originalPurchaseDate: sub.startsAt.toISOString(),       //     expirationDate: sub.expiresAt?.toISOString() || null,       //     store: sub.store as any,       //     productIdentifier: sub.productId,       //     ownershipType: 'PURCHASED',       //   };       // });              return { active, all: active };     },          // Verify webhook signature (security)     verifySignature(payload: string, signature: string, secret: string): boolean {       // In production: Use crypto.createHmac to verify RevenueCat webhook signature       // const expected = crypto.createHmac('sha256', secret).update(payload).digest('hex');       // return crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expected));       return true; // Mock for development     },   };   generator client {     provider        = "prisma-client-js"     previewFeatures = ["fullTextSearch"]   }      datasource db {     provider = "postgresql"     url      = env("DATABASE_URL")   }      // ============================================================   // Enums   // ============================================================      enum SubscriptionTier {     FREE     STARTER     PRO     ELITE     FAMILY   }      enum SubscriptionStatus {     ACTIVE     CANCELED     PAST_DUE     EXPIRED   }      enum BillingCycle {     MONTHLY     YEARLY   }      enum SubscriptionPlatform {     APPLE     GOOGLE   }      enum ScanStatus {     OPTIMAL     STABLE     RISKY     DAMAGING   }      enum GlycemicImpact {     LOW     MEDIUM     HIGH   }      enum HealthRole {     POSITIVE     NEUTRAL     CONCERNING   }      enum RiskLevel {     LOW     MEDIUM     HIGH   }      enum MissionType {     SCAN     SUGAR     HYDRATION     WATER_FIRST     CLEAN_DRINK   }      enum UserStage {     WEEK1     WEEK2     VETERAN   }      // ============================================================   // Models   // ============================================================      model User {     id           String    @id @default(cuid())     email        String    @unique     displayName  String?     avatarUrl    String?     provider     String    @default("email")     createdAt    DateTime  @default(now())     lastLoginAt  DateTime  @default(now())     streak       Int       @default(0)     lastScanDate DateTime?     stage        UserStage @default(WEEK1)        // Relations     scans         Scan[]     subscription  Subscription?     missions      Mission[]     analytics     Analytics?     feedbacks     UserFeedback[]   }      model Subscription {     id                    String               @id @default(cuid())     userId                String               @unique     user                  User                 @relation(fields: [userId], references: [id])        tier                  SubscriptionTier     @default(FREE)     status                SubscriptionStatus   @default(ACTIVE)     billingCycle          BillingCycle         @default(MONTHLY)        // RevenueCat + Cross-platform fields     platform              SubscriptionPlatform?     revenueCatCustomerId  String?     storeProductId        String?     storeTransactionId    String?     originalTransactionId String?        price                 Decimal              @db.Decimal(10, 2) @default(0)        currentPeriodStart    DateTime             @default(now())     currentPeriodEnd      DateTime?        canceledAt            DateTime?        createdAt             DateTime             @default(now())     updatedAt             DateTime             @updatedAt   }      model Scan {     id               String         @id @default(cuid())     userId           String     user             User           @relation(fields: [userId], references: [id])     imageUrl         String     detectedProduct  String     brand            String?     category         String     confidenceScore  Float          @default(0.0)     impactScore      Int            @default(0)     hydrationLevel   Int            @default(0)     glycemicImpact   GlycemicImpact @default(LOW)     scannedAt        DateTime       @default(now())     createdAt        DateTime       @default(now())     status           ScanStatus     @default(STABLE)        // Optional relations     compositionId String?     composition   Composition? @relation(fields: [compositionId], references: [id])     reportId      String?     report        Report?      @relation(fields: [reportId], references: [id])     aiInsightId   String?     aiInsight     AIInsightRecord? @relation(fields: [aiInsightId], references: [id])        sugarGrams Float?   }      model Composition {     id                   String       @id @default(cuid())     calories             Int          @default(0)     sugarGrams           Float        @default(0)     caffeineMg           Float        @default(0)     sodiumMg             Float        @default(0)     fatGrams             Float        @default(0)     proteinGrams         Float        @default(0)     additives            String[]     artificialSweeteners Boolean      @default(false)     servingSize          Float        @default(0)     servingUnit          String       @default("ml")        scan        Scan?     ingredients Ingredient[]   }      model Ingredient {     id            String      @id @default(cuid())     name          String     function      String?     healthRole    HealthRole  @default(NEUTRAL)     riskLevel     RiskLevel   @default(LOW)     description   String?     aiNote        String?     position      Int         @default(0)     compositionId String     composition   Composition @relation(fields: [compositionId], references: [id])   }      model Report {     id                         String @id @default(cuid())     shortTermEnergy            String     shortTermBloodSugar        String     shortTermBodyReaction      String     shortTermHydration         String     mediumEnergyStability      String     mediumPhysicalChanges      String     mediumHabitRisk            String     mediumSleepQuality         String     longTermHealthTrend        String     longTermMetabolic          String     longTermRiskAccumulation   String     longTermNutritional        String     viralStatement             String     tiktokHook                 String        scan Scan?   }      model AIInsightRecord {     id      String @id @default(cuid())     insight String     summary String     scan    Scan?   }      model Mission {     id           String      @id @default(cuid())     userId       String     user         User        @relation(fields: [userId], references: [id])     title        String     description  String     type         MissionType @default(SCAN)     target       Int         @default(1)     progress     Int         @default(0)     rewardScore  Int         @default(0)     rewardImpact Int         @default(0)     rewardText   String?     expiresAt    DateTime     completed    Boolean     @default(false)     completedAt  DateTime?     createdAt    DateTime    @default(now())   }      model Analytics {     id                String   @id @default(cuid())     userId            String   @unique     user              User     @relation(fields: [userId], references: [id])     totalScans        Int      @default(0)     avgImpactScore    Float    @default(0)     avgHydrationLevel Float    @default(0)     weeklySugarTrend  Float    @default(0)     energyCrashRatio  Float    @default(0)     consistencyTrend  Float    @default(0)     updatedAt         DateTime @updatedAt   }      model UserFeedback {     id        String   @id @default(cuid())     userId    String     user      User     @relation(fields: [userId], references: [id])     scanId    String?     scan      Scan?    @relation(fields: [scanId], references: [id])     feedback  String     createdAt DateTime @default(now())   }      // RevenueCat Webhook & Event Tracking   model RevenueCatEvent {     id            String   @id @default(cuid())     eventType     String     appUserId     String     productId     String?     revenueCatId  String?  // rc_xxx identifier if needed     payload       Json     processed     Boolean  @default(false)     errorMessage  String?     createdAt     DateTime @default(now())   }      // Optional: Subscription Audit Trail   model SubscriptionAudit {     id              String   @id @default(cuid())     subscriptionId  String     userId          String     action          String   // e.g. "CREATED", "RENEWED", "CANCELED", "STATUS_CHANGED"     oldStatus       SubscriptionStatus?     newStatus       SubscriptionStatus?     oldTier         SubscriptionTier?     newTier         SubscriptionTier?     metadata        Json?     createdAt       DateTime @default(now())   } `